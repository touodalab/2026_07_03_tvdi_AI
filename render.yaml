services:
  - type: web
    name: salary-predict-api
    runtime: python
    plan: free
    rootDir: backend/08_11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
